# Workspace Rules

- For prototype features, do **NOT** run browser automated subagent tests unless explicitly instructed by the user.
- The user handles browser testing manually and will share screenshots if feedback or adjustments are required.
- Card widths and layout dimensions can be increased/adjusted (e.g. `max-w-[360px]` or wider) as requested by the user.
- **Card Style 4**: Implemented as a **Dual-Pane Split Card** with a left gradient accent block (containing calendar icon & year badge) and a clean right pane for title, metadata, and action button.
